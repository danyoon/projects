var form = "<%=j render 'hotels/forms/send_user', hotels: @hotels %>";

$('body').append(form);

var modal = $('#hotel-send-user-modal');

modal.modal();
modal.on('hidden.bs.modal', function () {
  modal.remove();
})
