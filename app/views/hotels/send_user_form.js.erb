var form = "<%=j render 'microposts/forms/send_user', micropost: @micropost %>";

$('body').append(form);

var modal = $('#hotel-send-user-modal');

modal.modal();
modal.on('hidden.bs.modal', function () {
  modal.remove();
})
