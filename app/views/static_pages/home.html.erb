<% if signed_in? %>
  <div class="center hero-unit">
    <h1>AN EXPERIMENT.  A SUPPORT NETWORK TO HELP ENTREPRENEURS SUCCEED.</h1>
  </div>
  <div class="row">
    <aside class="span4">
      <section>
        <%= render 'shared/user_info' %>
      </section>
    </aside>
    <div class="span8">
      <section>
        <%= render 'shared/micropost_form' %>
      </section>
    </div>
  </div>
  <div class="row">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
  </div>  
<% else %>
  <div class="center hero-unit">
    <h1>AN EXPERIMENT.  A SUPPORT NETWORK TO HELP ENTREPRENEURS SUCCEED.</h1>
    <%= link_to "Sign up now!", signup_path, 
                                class: "btn btn-large btn-primary" %>
   <div class="row">
      <h3>Micropost Feed</h3>
      <%= render @users %>
  </div>
<% end %> 
