<% price = Price.for_date_and_hotel(firstDate, hotelfeed_price).first %>
<%= link_to image_tag(hotelfeed_price.image, :size => "80x50"), hotelfeed_price %>
<br>
<% unless (price.rate rescue nil).nil? %>
  <%= price.rate %>
  <%#= time_ago_in_words(price.created_at) %>
<% else %>
  0.0
<% end %>


