<div class="thumbnail" style="height: 400px">
  <% owners = Hotel.uniq.pluck(:owner) %>
  <% owners.in_groups_of(2).each do |owner_row|  %>
    <div class ="row">
    <div class="btn-group btn-group-lg btn-group-justified">
      <% owner_row.each do |owner| %>
      <%= link_to owner, root_url, onclick:'getURL(owner);', class:"btn btn-default", id:"companyLabel" %> 
    <% end %>
    </div>
  </div>
<% end %>
</div>
<div class="carousel-caption">
</div>

<script>
function getURL(owner) {
  var label = $('#companyLabel');
  label.attr('href', "/companies/" + owner);
}


</script>
