<div class="well">
    <%= firstDate.strftime("%a") %>
    <%= firstDate.strftime("%b %e") %>
    <hr>
    <%= render :partial => 'shared/summary_price', :collection => @summary_prices, :locals => { :firstDate => firstDate } %>
</div>
