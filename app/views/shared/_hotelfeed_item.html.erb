<li id="<%= hotelfeed_item.id %>">
<%= link_to image_tag(hotelfeed_item.hotel.image, :size => "80x50"), hotelfeed_item.hotel %>
    <span class="user">
      <%= link_to hotelfeed_item.hotel.name, hotelfeed_item.hotel %>
    </span>
    <span class="content"><%= hotelfeed_item.rate %> on <%= hotelfeed_item.date_for %> </span>
    <span class="timestamp">
      Posted <%= time_ago_in_words(hotelfeed_item.created_at) %> ago.
    </span>
  <% if current_user.admin == TRUE %>
    <%= link_to "delete", hotelfeed_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: hotelfeed_item.rate %>
  <% end %>
</li>
