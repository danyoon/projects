<% if @hotelfeed_prices.any? %>
  <ol class="prices">
    <% 14.times do |index| %>
      <% firstDate = Date.today + index.day %>
      <%= render :partial => 'shared/hotelfeed_item', :locals => { :firstDate => firstDate } %>
    <% end %>
  </ol>
<% end %>
