var form = "<%=j render 'hotels/forms/send_user' %>";

$('body').append(form);

var modal = $('#micropost-send-user-modal');

modal.modal();
modal.on('hidden.bs.modal', function () {
  modal.remove();
})
