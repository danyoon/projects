var form = "<%=j render 'microposts/forms/send_user' %>";

$('body').append(form);

var modal = $('#micropost-send-user-modal');

modal.modal();
modal.on('hidden.bs.modal', function () {
  modal.remove();
})
