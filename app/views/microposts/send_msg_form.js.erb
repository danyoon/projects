var form = "<%=j render 'microposts/forms/send_msg', micropost: @micropost %>";

$('body').append(form);

var modal = $('#micropost-send-msg-modal');

modal.modal();
modal.on('hidden.bs.modal', function () {
  modal.remove();
})
