<%= form_for (@micropost) do |f| %>
  <%= hidden_field_tag :to, params[:to] %>
  <%= hidden_field_tag :subject, params[:subject] %>
  <%= hidden_field_tag :message, params[:message] %>
  <%= f.submit "Send Disappearing Message", class: 'btn btn-lg btn-info' %>
<% end %>
