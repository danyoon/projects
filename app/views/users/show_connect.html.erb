<% provide(:title, @title) %>
<div class="container">
  <h1><%= @title %></h1>
  <% if @hotels.any? %>
    <ul class="thumbnails">
    <%= render @hotels %>
  </ul>
  <%= will_paginate @hotels %>
<% end %>
</div>

