<%= provide(:title, @title) %>
<div class="container">
  <h1><%= @title %></h1>
  <% if @users.any? %>
    <ul class="thumbnails">
    <%= render @users %>
  </ul>
  <%= will_paginate @users %>
<% end %>
</div>
